<?xml version="1.0" encoding="UTF-8"?>
<!--
  Runtime placeholders are expanded by `radio start` (or the legacy
  ../bin/start.sh). Keep credentials in config/secrets.env or the process
  environment, never in this file.

  The webroot/adminroot below are the Debian/Ubuntu locations. On Windows
  `radio start` rewrites both tags in the rendered runtime copy to the share
  directories inside the Icecast install; ICECAST_WEBROOT / ICECAST_ADMINROOT
  override that on either platform. Run "radio paths" to see which values the
  next start will use.
-->
<icecast>
  <location>Private station</location>
  <admin>admin@example.invalid</admin>

  <limits>
    <clients>64</clients>
    <sources>2</sources>
    <queue-size>524288</queue-size>
    <client-timeout>30</client-timeout>
    <header-timeout>15</header-timeout>
    <source-timeout>10</source-timeout>
    <burst-size>65536</burst-size>
  </limits>

  <authentication>
    <source-password>${ICECAST_SOURCE_PASSWORD}</source-password>
    <relay-user>relay</relay-user>
    <relay-password>${ICECAST_RELAY_PASSWORD}</relay-password>
    <admin-user>admin</admin-user>
    <admin-password>${ICECAST_ADMIN_PASSWORD}</admin-password>
  </authentication>

  <hostname>${ICECAST_HOSTNAME}</hostname>
  <listen-socket>
    <port>${ICECAST_PORT}</port>
  </listen-socket>

  <mount type="normal">
    <mount-name>/radio.mp3</mount-name>
    <max-listeners>50</max-listeners>
    <public>0</public>
  </mount>

  <fileserve>1</fileserve>
  <paths>
    <logdir>${RADIO_LOG_DIR}</logdir>
    <webroot>/usr/share/icecast2/web</webroot>
    <adminroot>/usr/share/icecast2/admin</adminroot>
    <alias source="/" destination="/status.xsl" />
  </paths>

  <logging>
    <accesslog>icecast-access.log</accesslog>
    <errorlog>icecast-error.log</errorlog>
    <loglevel>3</loglevel>
    <logsize>10000</logsize>
  </logging>

  <http-headers>
    <header name="Access-Control-Allow-Origin" value="*" />
  </http-headers>
</icecast>

