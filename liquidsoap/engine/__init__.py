"""Cross-platform radio engine manager (radio CLI).

A pure-stdlib Python lifecycle manager for the Liquidsoap + Icecast broadcast
chain.  Mirrors the behaviour of the legacy bash scripts in ``bin/`` but works
on both Linux and Windows.

See ``__main__.py`` for the CLI entry point and ``radio --help`` for usage.
"""

__version__ = "0.1.0"